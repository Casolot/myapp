<h2><%= @post.title %></h2>
<%= simple_format @post.body %>

<h3>Comments</h> 
<% if @post.comments.any? %>
<ul>
    <% @post.commnets.each do |comment|%> 
        <li><%=commnet.body %></li> 
    <%end%>
</ul>
<%end%>

<%= form_with model:[@post, @post.commnets.build] do |f| %>

<p> 
    <%= f.text_field :body %>
</p> 
<p> 
    <%= f.submit %>
</p> 
<% end %>